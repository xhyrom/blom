fun @native printf(format: string, ...) -> i32;

entity Person {
  i32 age;
}

fun Person::new(age: i32) -> Person {
  return Person {
    age = age
  };
}

fun Person::extend(self: Person) {
  printf("extended");
}

fun @public main() -> i32 {
  Person p1 = Person::new(42);
  return p1.age;
}
