fun @native printf(format: string, ...) -> i32;

entity Person {
  i32 age;
  i32 height;
  string name;
}

fun Person::new(age: i32, height: i32, name: string) -> Person {
  return Person {
    age = age,
    height = height,
    name = name
  };
}

fun @public main() -> i32 {
  Person p = Person::new(420, 55, "ahoj");;
  printf("age: %d\nheight: %d\nname: %s\n", p.age, p.height, p.name);
}
