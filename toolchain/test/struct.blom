fun @native printf(format: string, ...) -> i32;

entity Person {
  i32 age;
}

fun Person::new(age: i32) -> Person {
  return Person {
    age = age
  };
}

fun @public main() -> i32 {
  Person p1 = Person::new(420);
  printf("age: %d\n", p1.age);
  printf("age: %d\n", p1.age);

  p1.age = 985;

  printf("age: %d\n", p1.age);
}
