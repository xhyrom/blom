fun @native printf(format: string, ...) -> i32;

fun fib(n: i64) -> i64 {
    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }

    return 0;
}

fun main() -> i64 {
    printf("fib(50) = %d\n", fib(40))
    return 0;
}
